plot(
  data_group$temp,
  data_group$total_user,
  xlab = "Temperatura",
  ylab = "Total de usuários",
  main = "Relação entre temperatura e total de usuários"
) #gráfico

cor(data_group$temp, data_group$total_user) #0.6
